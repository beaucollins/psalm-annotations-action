{
  "name": "psalm-annotations-action",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "example": "example"
  },
  "scripts": {
    "check": "flow",
    "build": "babel --out-dir build report.js",
    "pack": "ncc build -o dist/ build/report.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "release": "npm run check && npm run build && npm run pack"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.2.2",
    "@actions/github": "^2.1.0",
    "@octokit/action": "^1.3.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.8.4",
    "@babel/core": "^7.8.4",
    "@babel/preset-env": "^7.8.4",
    "@babel/preset-flow": "^7.8.3",
    "@zeit/ncc": "^0.21.1",
    "flow-bin": "^0.118.0"
  },
  "babel": {
    "presets": [
      [
        "@babel/preset-env",
        {
          "targets": {
            "node": true
          }
        }
      ],
      "@babel/preset-flow"
    ]
  }
}
